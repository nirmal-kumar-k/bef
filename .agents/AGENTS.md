# Agent Rules & Constraints

- **Strict Adherence:** Never generate, assume, or hardcode business logic, fields, or placeholders on your own to "fill in the gaps" of the system.
- **Explicit Instruction:** Only build exactly what the user explicitly tells you to build. If data is missing for a workflow, ask the user instead of inventing it.
